# Get a key at https://typesafe.ai (Jev is served from the System One API).
TYPESAFE_API_KEY=your-key-here
# Optional: override the API host, e.g. a compatible gateway.
# TYPESAFE_BASE_URL=https://api.typesafe.ai
